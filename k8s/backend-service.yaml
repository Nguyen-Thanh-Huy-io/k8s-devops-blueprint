apiVersion: v1
kind: Service
metadata:
  name: ecommerce-backend-service
  namespace: ecommerce
spec:
  type: ClusterIP
  selector:
    app: ecommerce-backend
  ports:
  - name: tcp
    port: 8080
    targetPort: 8080
    protocol: TCP