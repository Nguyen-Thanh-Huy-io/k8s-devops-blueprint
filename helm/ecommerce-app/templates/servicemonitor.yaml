apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ecommerce-backend-servicemonitor
  namespace: ecommerce
  labels:
    release: thanhhuyvnW
spec:
  selector:
    matchLabels:
      app: ecommerce-backend 
  endpoints:
  - port: tcp
    path: /actuator/prometheus
    interval: 15s